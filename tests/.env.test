# Test environment – copy to .env.test and adjust for your local setup.
# These values are loaded by conftest.py before any app code runs.
TEST_DB_HOST=localhost
TEST_DB_NAME=pos_test_db
TEST_DB_USER=postgres
TEST_DB_PASSWORD=postgres
TEST_DB_PORT=5432

# Used by Playwright UI tests
APP_BASE_URL=http://localhost:5001
